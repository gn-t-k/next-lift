# ブランチ命名規則

許可されるプレフィックスとパターンは `.github/workflows/consistent-pull-request.yml` に列挙されている。意味の使い分けは以下:

- `feature/**/*` — **エンドユーザー向けの機能追加・挙動変更**
  - エンドユーザーが触れる動作・画面・生成 HTML が変わる変更
  - 例: 新規画面の追加、既存コンポーネントの挙動変更、フォームバリデーション追加
- `bugfix/**/*` — **エンドユーザー向けの不具合修正**
  - 例: 誤動作、クラッシュ、データ不整合の解消
- `refactor/**/*` — **振る舞いを変えない内部構造の整理**
  - 例: 命名変更、関数分割、型整理（エンドユーザーの動作・生成 HTML は不変）
- `infra/**/*` — **本番インフラ・運用環境の変更**
  - エンドユーザーのアプリが動く環境（外部サービス、ホスティング設定など）の変更
  - 例: Vercel / Turso / Sentry の設定、DNS、外部サービス連携
- `improve/**/*` — **開発環境・開発プロセスの改善**
  - 開発者だけが恩恵を受ける改善
  - 例: CI/CD ワークフロー、ビルド/Lint 設定、Storybook 環境、サブエージェント定義、`.claude/` 配下の更新
  - 「コンポーネントの挙動を改善する」は対象外（エンドユーザーが触れるので `feature/`）
- `chore/**/*` — **その他の雑務**
  - 上記いずれにも該当しない軽微な作業（リネーム、構成移動など）
- `deps/**/*` — **依存パッケージのバージョン更新**
  - 主に Renovate が使用
- `docs/**/*` — **ドキュメントのみの変更**
  - `docs/` 配下、README、ADR など

## 短縮形は禁止

`feat/`, `fix/` などの短縮形は CI (Consistent Pull Request) で弾かれる。必ず上記の長い形を使う。`gn-t-k/` のようなユーザー名プレフィックスも禁止。
