# 設計判断ログ

| # | フェーズ | 判断内容 | 選択肢 | 決定 | 理由 |
|---|---------|---------|--------|------|------|
| 1 | P1 | プログラム作成のユースケース粒度 | (a) 「プログラムを作成できる」で1つにまとめる (b) 作成方法・構造定義・種目配置を分割する | (b) 分割 | Pros: 各ステップが独立した操作として成立し、後続フェーズでのタスク・UI導出が容易。Cons: ユースケース数が増える。ただしシナリオ上も別ステップとして記述されており、粒度は適切と判断 |
| 2 | P1 | AI対話によるプログラム作成をユースケースに含めるか | (a) 初期スコープに含める (b) スコープ外として除外する | (b) 除外 | Pros: 初期スコープを小さく保てる。行動シナリオの「初期スコープ外」に「AIによるプログラム提案・サジェスト」が明記されている。Cons: 作成方法の選択肢が減る。ただしゼロから作成+コピーで最低限のニーズはカバー可能 |
| 3 | P1 | Dayスキップ・順序変更を独立ユースケースにするか | (a) 独立ユースケースにする (b) 既存ユースケースの範囲に含める | (b) 含める | Pros: 補足に「Dayのスキップ、順序変更、途中中断はすべて許容」とあるが、これはUI制約の解除（モードレス）であり独立した用途ではない。Cons: 明示されないと見落とすリスクがあるが、フレーム構造設計時に考慮すれば十分 |
| 4 | P1 | 動作主体の表記 | (a) ペルソナ名を使う (b) 「ユーザー」で統一する (c) ドメイン用語「トレーニー」を使う | (c) 「トレーニー」 | ユーザー確認済み: Better Authの`user`テーブルと区別するため、ドメイン層の動作主体を「トレーニー」（trainee）と呼ぶ。認証基盤のユーザーとドメインのプロフィールを分離する設計意図に合致 |
| 5 | P1 | 種目の登録・編集をユースケースに含めるか | (a) 種目CRUD用のユースケースを追加する (b) 含めない | (a) 一覧閲覧と名称変更を追加（UC_A_8, UC_A_9） | ユーザー確認済み: 種目名は人により呼び方が異なる（例: スモウデッドリフト vs ワイドデッドリフト）ため、プログラム作成と独立して種目名を変更したい場面がある。変更対象を選ぶために一覧も必要 |
| 6 | P1 | RPEを計画時の指定項目に含めるか | (a) 計画時にも指定可能にする (b) 記録時オプションのまま | (a) 計画時にも追加 | ユーザー確認済み: RPEベースのプログラム（重量固定×RPEで回数決定、回数固定×RPEで重量決定）を組むために、計画時にもRPEを指定できる必要がある |
| 7 | P2 | Feature分類の粒度 | (a) 行動シナリオの大区分（4つ）をそのままFeatureにする (b) 種目マスタを独立Featureに分離して5つにする | (b) 5つに分離 | Pros: UC_A_8/UC_A_9はプログラム作成と独立した文脈（種目名の呼び方変更など）で使われるため、独立Featureの方がタスクの関心事が明確になる。Cons: Feature数が増えるが、Functionが2つだけの小さなFeatureなので管理コストは低い |
| 8 | P2 | UC_D_1（1RM登録更新）を1つのFunctionにするか分割するか | (a) 「1RM登録更新」として1Function (b) 「1RM登録」(C)と「1RM更新」(U)に分割 | (b) 分割 | Pros: CRUDが異なる操作を1Functionにまとめると、後続フェーズでUIパターンの導出が曖昧になる。初回登録（C）と値の更新（U）はユーザーの意図が異なる。Cons: Function数が増えるが、CRUD分類の正確性を優先 |
| 9 | P2 | 振り返りのReadタスクをタスク表に含めるか | (a) Rは原則除外のルールに従い除外する (b) 明示的な分析・比較表示として含める | (b) 含める | Pros: 振り返りFeatureのタスクはすべてR（分析・比較表示）だが、単なる「データ表示」ではなく、集計・対比・推移計算を伴う明示的な分析タスクであるため除外すべきでない。除外すると振り返りFeature全体が消滅する。Cons: Rタスクが増えるが、これらは暗黙的な表示ではなく能動的な分析操作 |
| 10 | P2 | プログラム選択（UC_B_1）のCRUD分類 | (a) R（プログラムを読み込む） (b) CRUD対象外（選択行為自体はデータ操作ではない） | (b) CRUD対象外 | Pros: プログラム選択はデータのC/R/U/Dいずれでもなく、ユーザーがワークアウト記録の対象を指定するUI上の操作。CRUDを無理に割り当てると意味が曖昧になる。Cons: CRUD列が空になるが、全タスクがCRUDに分類される必要はない |
| 11 | P2 | Day構成編集・種目配置をプログラム編集と統合するか | (a) 統合して「プログラム編集」に含める (b) 独立Functionとして残す | (b) 独立 | Pros: UC_A_3/UC_A_4/UC_A_6はそれぞれ異なる操作対象（構造・種目・全体）を持ち、後続フェーズで異なるUIパターンに導出される可能性がある。独立していた方がタスク→UI対応が明確。Cons: 実際のUIでは「プログラム編集」の中にまとめられる可能性があるが、タスク整理段階ではFunction粒度を保つ方が安全 |
| 12 | P1/P2 | ユーザー作成データのCRUD網羅性 | (a) 行動シナリオに明示されたもののみ (b) 「ユーザーのデータはユーザーのもの」原則で全データにU/Dを保証 | (b) 原則適用 | ユーザー確認済み: ユーザーが登録したデータはすべて自由に編集・削除できるべき。ワークアウト削除(UC_B_6)・事後修正(UC_B_7)・1RM削除(UC_D_3)・種目登録(UC_A_10)・種目削除(UC_A_11)を追加 |
| 13 | P3 | 概念モデルに作成途中の状態を含めるか | (a) 含める（全関連を0始まりに統一） (b) 完成状態のみ表現する | (a) 含める | ユーザー確認済み: Day→計画セットで「作成途中」を理由に0..nとしていたのに、プログラム→Dayは1..nとしていた不整合を解消。全ての親子関連で作成途中の状態（0）を許容する方針に統一 |
| 14 | P3 | 期間（週数）をプログラムのプロパティとして持つか | (a) 持つ (b) 削除してDay名で表現 | (b) 削除 | ユーザー確認済み: 10/8/5プログラムのように期間が不定のプログラムが存在する。メソサイクル構造はDay名（例: "W1-D2"）で表現可能。必要ならメタ情報にフリーテキストで記録 |
| 15 | P3 | Day番号を持つかラベルを必須にするか | (a) Day番号を持つ（ラベルは任意） (b) ラベル必須にしてDay番号を削除 | (b) ラベル必須 | ユーザー確認済み: ラベルが必須なら識別子として機能する。デフォルト値（"Day 1"等）をシステムが提供し、ユーザーが採用or変更。順序は配列位置で保持 |
| 16 | P3 | 種目ごとのグルーピング層を導入するか | (a) Day→セット計画を直結 (b) 種目計画/種目記録を中間層として追加 | (b) 追加 | ユーザー確認済み: セット数が配列長で自然に表現される。種目ごとのメモ配置が可能になる。計画/記録の対称構造が振り返り（計画実績比較）を容易にする |
| 17 | P3 | 命名パターンの統一 | (a) 計画セット/記録セット（計画or記録が先） (b) セット計画/セット記録（{対象}{計画or記録}で統一） | (b) 統一 | ユーザー確認済み: 種目計画/種目記録、セット計画/セット記録で一貫した命名パターン |
| 18 | P3 | セット計画のパラメータ指定パターン | (a) 4プロパティ並列（レップ数, 重量, 重量指定方法, RPE） (b) 3つのうち2つを指定（{重量+回数}, {重量+RPE}, {回数+RPE}） | (b) 3つのうち2つ | ユーザー確認済み: ドメインルール「3つのうち2つを指定」を明示的に表現。重量指定はkg/%1RMの区別を含む |
| 19 | P3 | e1RMを概念オブジェクトとするか導出ビューとするか | (a) 概念オブジェクト (b) 導出ビュー | (b) 導出ビュー | ユーザー確認済み: e1RMはセット記録から純粋に算出可能な導出値。強度はe1RMだけでなく%1RMや単純な重量でも表現される。ボリュームも同種の導出値。保存不要で、ワークアウト削除時の連動削除問題も解消 |
| 20 | P3 | ワークアウト→Dayの多重度 | (a) 1→1（必須） (b) 1→0..1（任意） | (b) 任意 | ユーザー確認済み: 計画と紐づかないアドホックなトレーニング（マシンが空いていない等）に対応。0: アドホック、1: 計画Day準拠 |
| 21 | P3 | ワークアウト→プログラムの直接関連 | (a) 残す (b) 削除（Day経由で導出） | (b) 削除 | ユーザー確認済み: ワークアウト→Day→プログラムで導出可能。Dayなし（アドホック）のワークアウトはプログラムにも属さない。冗長な関連を排除 |
| 22 | P3 | 1RMにセット記録への参照を持たせるか | (a) 重量値のみ (b) セット記録への参照を追加（0..1） | (b) 追加 | ユーザー確認済み: 1RMの出どころ（実測 or e1RM採用）を追跡可能。アプリ外での申告値は参照なし（0） |
| 23 | P1/P3 | UC_C_2（重量推移確認）とUC_C_3（e1RM確認）を統合するか | (a) 個別UCのまま残す (b) 「強度・ボリューム推移確認」として統合しUC_C_3を削除 | (b) 統合 | ユーザー確認済み: Phase 3でe1RMを導出ビューに変更し、強度（e1RM・%1RM・重量）とボリュームを同じ導出ビュー群として整理した。ユースケースもこの分類に合わせて統合。重量推移もe1RMも強度の一側面であり、個別UCにする粒度ではない |
| 24 | P1/P3 | プログラム設計時の推移データ表示をユースケースに追加するか | (a) 追加しない（振り返りUCでカバー） (b) システムUCとしてUC_E_4を追加 | (b) 追加 | ユーザー確認済み: 振り返り（過去を振り返る）とプログラム設計時の参照（これから組む計画を調整する）はユーザーの目的が異なる。データは同じ導出ビューだが、操作コンテキストが異なるため別UC。UC_E_2（計画値プリフィル）と同類のシステム支援機能 |
| 25 | P4 | 最重要コンセプトの選定 | (a) プログラムを組む (b) トレーニングを記録する (c) トレーニングを振り返る | (a) プログラムを組む | Pros: 計画がなければ記録・振り返りのサイクルが成立しない。操作の幅が最も広くUI設計の難易度が最も高い。Cons: ユーザーが最も頻繁に使うのは記録（日常操作）だが、頻度と重要度は異なる。計画が存在して初めて記録・振り返りが意味を持つ |
| 26 | P4 | 種目マスタ・1RM管理のメンタルモデル上の位置づけ | (a) 提供価値と同列に並べる (b) 補助機能として分離する | (b) 補助として分離 | Pros: メインのサイクル（計画→記録→振り返り）と明確に区別でき、ナビゲーション設計時の優先度判断が容易になる。Cons: 補助と位置づけることでアクセス動線が深くなるリスクがあるが、ナビゲーション項目として独立させれば解消可能 |
| 27 | P4 | グローバルナビゲーション構造 | (a) タスクベース（計画する/記録する/振り返る/管理する） (b) オブジェクトベースの4タブ（プログラム/ワークアウト/振り返り/種目） (c) オブジェクトベースの2タブ + アカウントアイコン | (c) 2タブ + アカウントアイコン | Pros: OOUI原則に基づき、タスク指向の「振り返り」を独立タブから削除。振り返りはオブジェクト（プログラム・ワークアウト・種目）から到達可能。種目管理・1RM管理はアカウントアイコンから到達。タブ数が最小限で画面圧迫が少ない。Cons: 振り返りや種目管理への到達に1ステップ追加が必要だが、使用頻度を考慮すると妥当 |
| 28 | P4 | iOS/Webのレイアウト戦略 | (a) 統一レイアウト（レスポンシブ） (b) プラットフォーム別に最適化 | (b) 別最適化 | Pros: コンセプト定義の「コンテキスト依存の設計」に合致。計画時（Web）は高密度・俯瞰、記録時（iOS）は低密度・集中。同じレイアウトでは両方の要件を満たせない。Cons: 設計・実装コストが増える。ただしReact Native（iOS）とNext.js（Web）で技術スタックが分かれており、レイアウト共有の利点が薄い |
| 29 | P4 | ナビゲーション項目数 | (a) 2タブ（プログラム/ワークアウト）+ アカウントアイコン (b) 4タブ（プログラム/ワークアウト/振り返り/種目） (c) 3タブ（プログラム/ワークアウト/種目） | (a) 2タブ + アカウントアイコン | Pros: タブはコアサイクル（計画・記録）に集中。振り返りはオブジェクト（プログラム・ワークアウト・種目）経由で到達。種目管理・1RM管理・アカウント操作はアカウントアイコンから。粒度の異なるものを別導線に分離できる。Cons: 振り返りや種目管理への直接アクセスが失われるが、使用頻度を考慮すると適切 |
| 30 | P4 | 1RM参照先のワークアウト削除時の扱い | (a) 1RMごと削除 (b) 参照をnullにして重量値は保持 | (b) 参照null、値保持 | Pros: 1RMの重量値自体は有効であり、ユーザーが明示的に設定した値を消すべきではない。手動で削除・再設定する手段がある（UC_D_1, UC_D_3）。Cons: 算出元の追跡ができなくなるが、1RMの主目的は現在の最大挙上重量を知ることであり、出どころ追跡は補助的 |
| 31 | P4 | グローバル検索の必要性 | (a) グローバル検索を設ける (b) 設けない（各一覧内のフィルターで対応） | (b) 設けない | Pros: ユーザーは自分が登録した種目名・プログラム名を知っている（能動的なユーザー）。データ量もパーソナルユースのため少ない。各一覧内のフィルターで十分。Cons: データ量が増えた場合にスケールしない可能性があるが、パーソナルアプリのため現実的な上限は低い |
| 32 | P4 | 種目詳細ビューの独立性 | (a) 種目一覧にインライン展開 (b) 独立した種目詳細ビューを設ける | (b) 独立ビュー | Pros: 種目名編集 + 1RM管理4操作を集約でき、拡張ポイントになる。推移グラフへの導線も配置可能。Cons: ビュー数が増えるが、種目起点の操作集約地点として妥当 |
| 33 | P4 | ワークアウト記録中のフルスクリーンモード | (a) ボトムタブバーを表示したまま (b) ボトムタブバーを非表示にしてフルスクリーン | (a) タブバー表示のまま | Pros: モードレス原則に合致。自動保存により誤遷移してもデータが失われない。2タブ構成でタブバーの画面圧迫が最小限。Cons: 画面領域がやや狭くなるが、2タブなら影響は軽微 |
| 34 | P4 | 種目削除時のUI方針 | (a) 完全削除 + 削除時警告 (b) アーカイブ（非表示+フィルターで再表示可） | (a) 完全削除 + 削除時警告 | Pros: 「削除」の操作が明快。アーカイブという追加概念が不要でユーザーの学習コストが低い。警告ダイアログで意図しない影響を事前に通知できる。Cons: 完全削除のため取り消しが効かないが、警告ダイアログで確認を求めることでリスクを軽減 |
| 35 | P4 | 振り返りタブの削除 | (a) 振り返りを独立タブとして残す (b) 振り返りタブを削除し、オブジェクト経由で到達 | (b) タブ削除 | Pros: OOUI原則に基づき、タスク指向の「振り返り」を削除。計画実績比較はプログラム/ワークアウト経由、推移は種目詳細経由で到達可能。オブジェクト中心のナビゲーションに統一される。Cons: 振り返りへの直接アクセスが失われるが、振り返りは必ず何らかのオブジェクト（プログラム・ワークアウト・種目）を起点とするため、オブジェクト経由の到達が自然 |
| 36 | P4 | プラットフォーム配分方針 | (a) ワークアウト記録をiOS限定にする (b) 基本操作はWeb/iOS両対応、デバイス依存体験のみ限定 | (b) 両対応 | Pros: 基本操作（プログラム作成・ワークアウト記録・振り返り）をWeb/iOS両方で可能にすることで、プラットフォームに依存しない体験を提供。Cons: 設計・実装コストが増えるが、コンテキスト依存の設計方針（計画時: 大画面、記録時: 小画面）に沿ったプラットフォームごとの体験差は維持する。デバイス依存体験（カメラ、ハプティクス等）はiOSのみ、広画面必須ビューはWebのみ |
| ~~37~~ | ~~P4~~ | ~~ナビゲーション項目の命名~~ | - | ~~(b) 日本語名称~~ | #50 で撤回（タブ名は概念オブジェクト名に統一） |
| 38 | P4 | アカウント管理の配置 | (a) 独立タブとして配置 (b) ナビゲーションバーのアカウントアイコンから到達 | (b) アカウントアイコン | Pros: ボトムタブとは粒度が異なる操作（種目管理・1RM管理・サインアウト・アカウント削除）を別導線に分離できる。タブ数を最小限に保てる。Cons: 種目管理・1RM管理への到達が1ステップ増えるが、使用頻度を考慮すると適切 |
| 39 | P4 | UC_A_3の名称変更 | (a) 「メソサイクル構造定義」のまま (b) 「Day構成編集」に変更 | (b) Day構成編集 | Pros: 操作の実態（Dayの追加・削除・ラベル変更）に合った名前で、ユーザーにとって直感的。「メソサイクル」はドメイン用語としてやや専門的。Cons: ユースケース名を変更するため、関連ドキュメントの全箇所を更新する必要がある |
| 40 | P5 | ワークアウトタブのランディング構成 | (a) V5（ワークアウト開始）とV8（ワークアウト履歴）を別ビューにする (b) 1つのビュー内のセクション構成で統合する | (b) 統合 | Pros: ワークアウトタブを開いた直後に「今日やること」と「過去の記録」の両方が見える。1タップでワークアウト開始でき、過去の振り返りにもスクロールで到達可能。Cons: 情報量が多くなるが、ワークアウト開始を上部に大きく配置し履歴は下部リストで控えめに表示することで優先度を表現 |
| ~~41~~ | ~~P5~~ | ~~ワークアウト完了サマリー（V7）からの遷移先~~ | - | ~~(b) 両方提供~~ | #46 で撤回（V7 自体を廃止しトースト通知に変更） |
| ~~42~~ | ~~P5~~ | ~~プログラム作成（V14）完了後の遷移先~~ | - | ~~(b) V2 に自動遷移~~ | #53 で V14 撤廃により無効化（その後 #54 で新「プログラム新規作成」ビューが復活） |
| 43 | P5 | Day詳細の兄弟間ナビゲーション | (a) 兄弟移動なし（プログラム詳細に戻ってDay再選択） (b) タブ切り替え/スワイプでDay間を直接移動 | (b) 直接移動 | Pros: Phase 4でDayをタブ状に並べる方針と整合。プログラム内のDay間を素早く移動でき、計画の俯瞰・比較が容易。Cons: 実装の複雑さが増すが、Day構成がタブ表現であるため自然な操作 |
| 44 | P5 | 種目詳細（V13）への横断到達 | (a) 種目一覧（V12）からのみ到達可能 (b) 種目計画・種目記録の種目名タップでも到達可能 | (b) 複数経路 | Pros: 縦割り構造を回避し、どの文脈からでも種目にアクセスできる。種目はプログラム・ワークアウト・アカウントのどこからでも参照されるオブジェクトであり、参照元からの直接到達が自然（Phase 4の方針と合致）。Cons: 呼び出し元に応じた戻り先管理が必要だが、ナビゲーションスタックで対応可能。補足: V6（セット記録）からの横断は、記録中の集中を守るため視覚的に控えめな表現にする（導線自体は維持） |
| 45 | P5 | ワークアウト詳細（V9）の兄弟間ナビゲーション | (a) 時系列で前後のワークアウトに移動可能にする (b) 兄弟移動なし | (b) なし | Pros: ワークアウトは時系列で並ぶだけで構造的な親子関係がなく、隣接ワークアウトが同じプログラムに属する保証がない。ワークアウト間の比較は計画実績比較（V10）や推移グラフ（V11）で対応可能。Cons: ワークアウト履歴から到達した場合のブラウズ性がやや下がるが、一覧に戻って選択する動線で十分 |
| 46 | P5 | ワークアウト完了サマリー（V7）の廃止 | (a) V7を独立ビューとして残す (b) V7を廃止し、V6完了後にV5+V8へ戻る＋トースト通知でV9への導線を提供 | (b) 廃止+トースト | Pros: V7はユーザーを一旦止める「チェックポイント」でありモードレス原則に反する。トーストならユーザーが自由に次の行動を選べる（無視して次のワークアウト開始、詳細確認、いずれも可能）。ビュー数が減り構造がシンプルになる。Cons: ワークアウト完了の達成感演出の機会が減るが、トーストで最低限のフィードバックは提供される |
| 47 | P3 | 1RM削除時の%1RMセット計画への影響 | (a) %1RM指定を自動的にkg指定に変換する (b) %1RM指定を保持しUI警告で対応 (c) 1RM削除をブロックする | (b) 保持+警告 | ユーザー確認済み: 計画データを壊さず、1RM再設定で即座に計算可能に戻る可逆性を重視。削除時に影響範囲を事前通知する（種目削除#34と同パターン）。%1RM指定の値自体は有効であり、自動変換やブロックよりユーザーの意思を尊重する方針 |
| 48 | P6 | Feature一覧の1RM Function数の修正 | (a) 4のまま (b) 5に修正 | (b) 5に修正 | Pros: 設計判断#8で1RM登録と1RM更新を分割した結果、Function数は5が正確。タスク表の実態と一致させることで後続の実装計画で齟齬が生じない。Cons: なし |
| 49 | P6 | タスク表「Day構成編集」の旧表現の修正 | (a) 「期間（週数）」を残す (b) 削除する | (b) 削除 | Pros: 設計判断#14で期間（週数）はプログラムのプロパティから削除済み。タスク表の記述を設計判断と一致させることで整合性が保たれる。Cons: なし |
| 50 | P6 | ナビゲーション項目名と概念オブジェクト名の統一 | (a) タブ名に「計画/記録」を使い概念オブジェクト名と区別する (b) タブ名と概念オブジェクト名を統一する | (b) 統一 | ユーザー確認済み: 対象ユーザーは中級・上級トレーニーでドメイン用語を理解している。UI・概念オブジェクト・コード（英語）の全レイヤーで同じ用語を使うことで翻訳コストを排除。タブ名は「プログラム」「ワークアウト」 |
| 51 | P6 | 概念オブジェクト「セッション」の名称変更 | (a) セッション (b) ワークアウト (c) トレーニング (d) ログ | (b) ワークアウト | ユーザー確認済み: 対象ユーザー（中級・上級トレーニー）の実感として「セッション」は日常的に使わない用語。「ワークアウト」はトレーニーに馴染みがあり、フィットネスアプリの業界標準（Apple Watch等）。英語対応（workout）も自然。「1回の実施とその記録」という概念を過不足なくカバーする |
| 52 | P6 | e1RM自動算出のCRUD分類 | (a) C（Create: 値の生成） (b) R（Read: セット記録からの読み取り・算出） | (b) R | ユーザー確認済み: 設計判断#19でe1RMを導出ビューに変更した結果、e1RM算出は概念オブジェクトのCreateではなくセット記録の読み取り・算出処理。Rが実態に合致 |
| 53 | P5 | プログラム新規作成のビュー構造 | (a) V14（プログラム作成）を独立ビューに残し、V1→V14（名前フォーム）→V2（Day等）のフローにする (b) V14を撤廃し、V1→V2直接遷移＋V2のインライン編集で名前入力する | (b) V14撤廃 | Pros: 既存編集（V2のインライン編集）と新規作成で同じUI（インライン編集）に統一でき、`design-principles.md`「発見の設計: 一貫性」と整合する。設計判断#11 Cons「実際のUIでは「プログラム編集」の中にまとめられる可能性がある」を実現。設計判断#42の趣旨（ステップが少ない方が良い）をさらに進めV1→V2の1ステップに短縮。Cons: V2が name 空の Program を扱う必要が生じるが、`design-principles.md` L37「初回はサンプルコンテンツで触って発見させる」と整合し、V1→V2遷移時に name 空のレコードを生成（または lazy 生成）する設計で対応可能。設計判断#42を撤回、V14は単位ビュー一覧から削除 → #54で「V2インライン編集統合」部分を再判断し、新「プログラム新規作成」ビューを独立フォームとして復活（旧V14とは別物） |
| 54 | post-5 | プログラム新規作成のビュー構造（#53再判断） | (a) #53の通りV2のインライン編集に統合する (b) プログラム新規作成を独立ビュー（フォーム形式）として復活させる | (b) 独立ビューとして復活 | Pros: V2のインライン編集統合だと毎フィールドで「編集モードに入る」操作が必要で手間。プログラム新規作成は複数フィールド（プログラム名 + 詳細）と将来のAI生成連携（プログラム名+詳細を入力→「手動で入力」or「AIで生成」を選択→Day新規作成へ）を見越すとフォーム形式が自然。「プログラム名+詳細」セクションと「Day等構造」セクション（=Day新規作成、#55）を分離することでAI連携の動線を確保できる。Cons: ビュー数が増えるが、新規作成と編集でUIを目的特化できるメリットが上回る。#53のPros「インライン編集の一貫性」は単一フィールド前提だったが、複数フィールド+AI連携を見越すと別ビューが合理的。#53のV14撤廃自体は維持し、新「プログラム新規作成」は旧V14とは別物（旧V14はプログラム名のみのシンプルフォーム） |
| 55 | post-5 | Day作成のビュー構造 | (a) プログラム詳細内のインライン編集で完結 (b) Day新規作成を独立ビュー（フォーム形式）として導入 | (b) 独立ビュー導入 | Pros: #54と同じロジックでフォーム形式が手間少。プレースホルダー値（Day名「Day 1」、種目未選択の種目計画×1、値未入力のセット計画×1）で「触って発見」（`design-principles.md` L37）を構造的に実現。プログラム新規作成→Day新規作成→プログラム詳細のフロー（Googleフォームのセクション切替体験）が一貫。Cons: ビュー数が増えるが、新規作成系の独立性を保つメリットが上回る。データ構造はerd-design設計判断#32（`set_plans → params`を`\|\|--o\|`に緩める）で対応 |
| 56 | post-5 | Day詳細とプログラム詳細の関係 | (a) Day詳細を独立ビューとして残す (b) プログラム詳細に統合し、Day選択はプログラム詳細内のタブ切替で完結 | (b) 統合 | Pros: プログラム詳細がプログラム名・詳細・Day（タブ）・種目計画・セット計画を1画面でセクション分けず表示するなら、Day詳細を独立ビューに分ける理由がない。設計判断#43（Day兄弟移動はタブ切替/スワイプ）と同じくDay構成のタブ表現で完結。ビュー数を削減できる。Cons: 1画面の情報量が増えるが、Dayはタブで切り替えるためスクロール量は限定的 |
| 57 | post-5 | 種目選択ビュー（V4）の扱い | (a) V4種目選択を独立ビューとして残す (b) 撤廃しプログラム詳細内のComboBox（広画面=Popover/狭画面=Drawer）に集約 | (b) 撤廃 | Pros: 種目選択 = ComboBox（検索 + 選択 + 新規作成）で広画面・狭画面を吸収可能（狭画面はDrawer形態でモバイルにも対応）。種目を選ぶ操作のためだけの別ビューは過剰。設計判断#53のV14撤廃と同じロジック（専用フォームよりインライン操作）。種目マスタ管理（設計判断#32の種目詳細・種目一覧）は別目的で残るので機能の重複なし。Cons: ComboBoxのDrawer形態の実装コストはあるが、専用ビュー実装と相殺される |
| 58 | post-5 | 新規作成時の初期構造とアプリ層責務 | (a) Programのみ作成、その後ユーザーがDay/種目計画/セット計画を順に追加 (b) Program + Day×1 + 種目計画×1（種目未確定） + セット計画×1（値未入力 = paramsなし）を初期生成 | (b) 初期構造込みで生成 | Pros: ユーザーの「次に何を入力するか」が構造的に明示され、`design-principles.md` L37「触って発見させる」と整合。Day新規作成（#55）の空フォームで「種目未選択のComboBox + 値未入力のセット計画行」をプレースホルダーとしてUI上に見せられる。Cons: 「保存されている空のデータ」が増えるが、ユーザーがそのまま離脱しても構造として残るのは「触って発見」の一貫性として許容範囲。アプリ層（apps/web）の責務として実装。データ構造側の対応はerd-design設計判断#32（`set_plans → params`の`\|\|--\|\|`を`\|\|--o\|`に緩める）を参照 |
| 59 | post-5 | セット計画の操作仕様 | (パターン切替単位) (a-i) セット単位 (a-ii) 種目計画単位 / (パターン切替時の値) (b-i) 全クリア (b-ii) 軸が共通する値を維持 / (セット追加時の継承) (c-i) 直前セットの値とパターンを継承 (c-ii) 初期値で生成 / (種目計画追加時の継承) (d-i) 直前種目計画の末尾セットを継承 (d-ii) 初期構造にリセット / (微調整UI) (e-i) なし (e-ii) `-2.5kg/+2.5kg/-1回/+1回`ステッパー | (a-i) セット単位 / (b-i) 全クリア / (c-i) 直前セット継承 / (d-ii) 初期構造リセット / (e-ii) ステッパーあり（刻み量は種目スキーマで持つ） | Pros: 高重量低回数+最終セットRPE評価のような混在ユースケースに対応するためパターン切替はセット単位（a-i）。パターン切替時に軸が共通する値を維持（b-ii）は混乱を招くため全クリア（b-i）。セット追加は「直前と同じか少しずらす」が大半なので直前のセットの値とパターンを継承（c-i）し、入力負担を軽減。種目が違えば適切な値も違うため種目計画追加時はリセット（d-ii）。微調整ステッパー（e-ii）は連続セットでの値ずらしの入力負担をさらに軽減。Cons: 刻み量はバーベル系=2.5kg/ダンベル系=1kg等で異なるため、固定値ではなく種目スキーマで持つ必要あり（`exercises`に`weightStep`/`repsStep`カラム追加。ERD変更は別Issueで追跡） |
| 60 | post-5 | 編集モードの操作仕様（参照系ビュー） | (入る) (a-i) フィールドタップで自動 (a-ii) 鉛筆アイコン明示タップ / (抜ける確定) (b-i) blurで自動 (b-ii) Enterキー (b-iii) チェックアイコン明示タップ / (キャンセル) (c-i) Escape | 入る=(a-ii)鉛筆アイコン / 確定=(b-ii)Enter or (b-iii)チェックアイコン / キャンセル=(c-i)Escape / blur=何もしない（モード維持） | Pros: 編集モード入る/抜けるUIを対称的なアイコン（鉛筆/チェック）で明示することで状態遷移が分かりやすい。blur確定は誤タップで意図しないコミットが起きるため除外。Enterキー確定は物理キー操作の慣習。Cons: なし。本仕様の適用対象は参照系ビュー（プログラム詳細・種目詳細）のみ。新規作成系ビュー（プログラム新規作成・Day新規作成）はフォーム全体を監視し全体validのタイミングで都度保存する別仕様（編集モード概念なし）。breakdown.md L120-128の「タップ→input→blurで確定」純インラインパターンは本判断で上書き |
| 62 | post-5 | セット計画編集UIにパターン切替を持たせるか（#59 (b) 再判断） | (a) 編集UIにパターン切替を持つ（#59 (b-i) で切替時に値全クリア） (b) 編集UIから撤去し、パターン変更はセット削除→追加で行う | (b) 撤去 | Pros: パターン切替時の値クリア（#59 (b-i)）による暗黙のデータ消失が無くなる。削除という重めの操作になることで「本当にこのパターンを捨てるか」のセルフチェックが自然に入り、ユーザー意図と操作結果が一致する。編集フォーム内のインタラクティブ要素（切替ボタン + メニュー + メニュー項目）が丸ごと不要になり、「切替ボタンが何に作用するか分かりづらい」「配置がフィールドから離れている」という現状の不満も同時に解消される。種目計画内のパターン混在ユースケース（5/3/1 プログラムの最終セット AMRAP など）は実在するため、SetPlan 単位でパターンを保持する #59 (a-i) の構造は維持。ただし混在頻度自体が低いため、専用切替UIをセット編集側に持つ価値は薄い。パターン指定の責務はセット追加側（#59 (c-i) の「直前セット継承」を既定とし、別パターンで追加したい場合は追加 UI でパターンを選ぶ）に寄せる。Cons: パターン変更時の操作コストが「メニューから1クリック」から「削除→追加→値再入力」に増える。ただし混在頻度の低さから許容範囲。設計判断 #59 の (b-i) パターン切替時の値全クリア は本判断で moot（編集側で切替自体が発生しなくなる） | → #69 で部分撤回（Tabs UI なら値保持で kind 切替できるため、編集にも Tabs を導入） |
| 69 | post-5 | 編集 UI にも Tabs で kind 切替を許可するか（#62 部分撤回） | (a) #62 のまま編集 UI から kind 切替を排し、kind 変更は削除→追加で行う (b) 編集 UI にも Tabs を入れ、kind 切替時は値を保持する（追加 UI と同じ Tabs+form ダイアログを共用） | (b) 編集にも Tabs、値保持 | Pros: #62 は「kind 切替時の値クリア（#59 (b-i)）が暗黙のデータ消失を起こす」を主な根拠としていたが、それはトグルボタン UI 前提だった。Tabs は「並行する状態を切り替えて表示する」（ブラウザタブのような）メタファであり、各タブが独立した値を保持しているのが自然。「切り替える」という確定的なアクションを伴うトグルとは違い、Tabs では値保持に違和感が出にくい。#68 で追加 UI を Tabs+form ダイアログに統合した結果、追加と編集で UI が違うミスマッチが残っていたが、本判断で完全に統一される。各 kind 別 Row が抱えていた重複 draft 管理（useState / useRef / writeDraft / handleCommit / Tabs+form 構造）はダイアログ側に一元化でき、Row は「表示 + 編集トリガー + 削除」だけの薄い責務になる。onChange の API も `{ weight, reps }` のような kind 個別ペイロードから `SetPlanWithParams` （kind 込み）に統一され、SetPlanSection 側の handler も pattern 組み立てが不要になり簡素化される。Cons: #62 が懸念した「kind を捨てる操作のセルフチェック」は失われるが、Tabs はタブを切り替えるだけ（明示的な「変える」アクションではない）であり、確定ボタンを押さなければコミットされないので事故の余地は限定的。値保持により「重量×回数で 100kg/5回 入力 → 重量×RPE タブに切り替えると重量 100 は保持、rpe は null」という挙動になり、ユーザーは追加情報の入力に集中できる |
| 63 | post-5 | セット計画 Row コンポーネントを kind 別に分割するか（#62 派生） | (a) 単一の `SetPlanRow` が `pattern.kind` で内部分岐 (b) kind ごとに独立した Row コンポーネント（`SetPlanRowWeightXReps` / `SetPlanRowWeightXRpe` / `SetPlanRowRepsXRpe`）に分割 | (b) kind 別に分割 | Pros: #62 で編集 UI 内のパターン切替が消え kind が編集中に切り替わらなくなり、内部 discriminator 分岐の正当性が消失。`.claude/rules/coding-style.md` の「フラグ引数アンチパターン」に整合。SetPlanSection が一度だけ `pattern.kind` で switch して呼び分け、各 Row は kind 不要の個別フィールドと `onChange` を持つ単一責任になる。kind 別の Storybook で play test が抜け落ちにくい。Cons: ファイル数が増えるが、共通レイアウト（`SetPlanRowFrame`、Popover/Drawer ラッパー、汎用 EditTrigger）に internal helper でまとめてカバー。注: 当初は `pattern: null` の状態を表示のみの `SetPlanRowEmpty` で扱う方針も付随していたが、#68 で Empty 自体が廃止された |
| ~~64~~ | ~~post-5~~ | ~~セット計画編集の auto-save 化（#60 再判断）~~ | - | ~~(b) auto-save~~ | #65 で撤回（実装後に「編集中に背後の値が変わるのが違和感」「編集前の値が分からない」体験問題が判明） |
| 65 | post-5 | セット計画編集の auto-save を撤回するか（#64 事後撤回） | (a) #64 の auto-save を維持する (b) #64 を撤回し、#60 の確定ボタン方式（draft 管理 + Enter / チェックアイコンで確定 + Escape で破棄）に戻す | (b) 撤回して確定ボタンに戻す | Pros: 実装してみて auto-save には「編集時に現在の値とフォームの値が両方書き換わる UI に違和感がある」「編集前の値がなんだったのか分からない（特に Popover で背後の値もリアルタイムに変化する）」という体験上の問題が判明した。確定ボタン方式なら編集前の値が画面上に保持され、ユーザーは編集中の値と比較しながら確定を判断できる。実装的にも auto-save で必要だった保存処理のデバウンス・楽観更新・部分失敗時のロールバックといった考慮事項が消える。Cons: #64 で挙げた「draft 管理の重複コード」「確定ボタンの DOM」「操作ステップが 1 つ増える」は再び発生するが、共通の `SetPlanRowEditTrigger` に draft 管理と確定動線を寄せることで重複コードは抑えられる。操作ステップ増は「現在値の温存」というメリットの裏返しであり、参照系ビュー（編集モード概念あり、設計判断 #60）の本来の挙動として許容する。新規作成系ビュー（V16/V17）の「フォーム全体 valid で都度保存」とは別仕様という #60 の整理を維持 |
| 68 | post-5 | Empty 行を廃止して「セットを追加」に統合するか（#58 再判断） | (a) Empty 行（#58 で導入）+ Tabs+form ダイアログ運用と `CreateSetPlanRow（プレビュー値 + パターントグル）` の 2 種類の UI を維持 (b) #58 の「初期セット 1 件生成」を撤回し Empty 行を廃止。`CreateSetPlanRow` を「セットを追加」シンプル文言の破線枠アフォーダンスボタンに統一し、押すと Tabs + 値入力 + 確定ボタンのダイアログを開く（直前セットがあればその kind と値を初期値として継承） | (b) Empty 廃止 + Tabs+form ダイアログに統合 | Pros: `design-principles.md` の (1) L7「アプリの存在を忘れて計画に没入」: ボタンの中身（プレビュー値）を読まなくても目的（追加）で押せる、(2) L38「1,2,3 がわかれば 4 を予測できる」: 編集も追加も「ダイアログで値を入れて確定」の同じメンタルモデル、(3) L39「大胆な取捨選択 / 目的達成への一直線」: パターントグル（↔）/ プレビュー値ラベルといった迂回 UI を撤廃、すべて Tabs+form 1 経路 と整合する。Empty 行運用で生じていた「Empty があるとき追加ボタン非表示」という排他的表示ロジック、`SetPlan` 型の `{ pattern: null }` ケース、`SetPlanRowEmpty` コンポーネント、`hasEmptyRow` 判定が全て構造的に消える。#58 の「サンプルコンテンツで触って発見」は「破線枠 + 『セットを追加』のアフォーダンス」で十分実現でき、`SetPlan` のデータが空のままダングリングする状態がなくなる方がデータモデル的にもクリーン。#59 (c-i)「セット追加時の直前セット継承」は Tabs+form ダイアログの事前入力ロジックとして引き継ぐ。#59 (e-ii)「ステッパー」は Field の中身として維持。Cons: 「同じ値でセット追加」が 1 タップ（プレビュー直接タップ）から 2 タップ（追加ボタン → 確定）に増える。ただし計画時ビュー（V2、ジム外）なのでミリ秒単位の速度より一貫性を優先する判断（記録時 V6 は別仕様）。Cons: `SetPlanRowEmpty` 関連の play test / ストーリー / 型定義の撤去コストが発生 | → #70 で部分撤回（1 件以上あるときはダイアログを開かずクイック追加に分岐） |
| 70 | post-5 | セット計画追加：1 件以上あるときダイアログを開かずクイック追加に分岐するか（#68 部分撤回） | (a) #68 のまま追加導線は全状態で「セットを追加」→ Tabs+form ダイアログを開く 1 経路 (b) `setPlans.length === 0` のときのみダイアログ、1 件以上あるときは直前セットの値をラベルに埋めたボタン（`#N 100kg × 10回を追加` のような前値明示ラベル）を表示し、押下で即 `onAdd`（ダイアログを開かない）。違う値で追加したいケースは、追加 → 行の鉛筆ボタンから既存の編集ダイアログに入る動線で吸収 | (b) クイック追加に分岐 | Pros: #68 で cons として挙げていた「1 タップ → 2 タップ」が、ストレートセット中心の利用（プログラム内に同じ値のセットが連続する RPT / 5×5 / BBB 等）で支配的に効く。毎回ダイアログを開いて確定を払う冗長さが消え、設計原則 L7「アプリの存在を忘れて計画に没入」と整合。クイック追加ボタンのラベルに前値を明示する（`100kg × 10回を追加`）ことで「押したら何が起きるか」の予測可能性は維持され、また「セットを追加」のような匿名ラベル（値を読まなくても押せる #68 の利点）と比べても「明示ラベルだから違う値を入れたい場合に気付ける」という別種の予測可能性が得られる。違う値で追加したい少数派ケースは「追加 → 鉛筆で編集」の +1 タップで対応する（鉛筆ボタンは各行に常設され #69 の編集ダイアログに繋がるため発見性は確保）。#68 が確立した「追加初回（0 件）も編集も Tabs+form ダイアログ 1 経路」の一貫性は維持され、quick add は「2 件目以降の連続追加」というスペシャライズドパスとして上乗せされる構造。Cons: 同じ位置のアフォーダンスが状態（0 件 vs 1 件以上）でラベル・挙動を変えるため、初見ユーザーが 0 件時のダイアログ経験から連続追加で「あれ、ダイアログが開かない？」となる可能性はある。ただしボタンラベルが値を含むため「これを押すと前値で追加される」ことは推察可能で、許容範囲。Cons: 違う値で追加したい場合の操作数が増える（追加 → 鉛筆編集）。ただし計画時ビュー（V2）はジム外でじっくり組むコンテキストなのでミリ秒単位の差は重要でない |
| 71 | post-5 | 種目計画追加・削除の API と V2 における初期構造の責務 | (a) `onAddExercisePlan` を引数なしで定義し、初期構造（`exercise: null` + `setPlans: []`）の生成は react-components 側で予約済みの shape として表現する (b) `onAddExercisePlan(dayId)` のみ受け取り、初期構造の生成・ID 採番は consumer (apps/web) の責務に閉じる。種目計画の削除はカード右上角に `XMarkIcon` ボタンを `absolute` で配置して `onDeleteExercisePlan(id)` を発火させる | (b) consumer 責務 + カード右上 × ボタン削除 | Pros: 設計判断 #58「アプリ層が初期構造を生成する責務を持つ」と完全に整合。`react-components` は ID 採番もストレージも持たないため、初期構造を component 側に押し込むと結局 `consumer が後から書き換える` 二重実装になる。設計判断 #68 で Empty 行が廃止された結果、種目計画追加直後の初期構造は `setPlans: []`（プレビュー値ボタン経路、`setPlans.length === 0` のときは Tabs+form ダイアログ）に単純化できる。Issue #776 が指摘していた「種目計画追加直後にセット計画 0 件のため `0kg × 0回` 既定値で追加するしかない」問題は、#68（Empty 廃止 + Tabs+form ダイアログ）と #70（quick add 分岐：0 件のときは Tabs+form ダイアログ）で構造的に解消済みのため、本判断のスコープはあくまで「種目計画レベルの追加・削除 API」に閉じる。削除は `intent="plain"` `size="sq-xs"` の `XMarkIcon`（`@heroicons/react/24/solid`、Modal / Drawer の閉じるボタンと同じ語彙）をカード（`Section`）の右上角に `absolute top-2 right-2` で独立レイヤーとして配置し、ヘッダー側には `pr-8` で重なり回避の余白を確保する。1 タップで `onDeleteExercisePlan(id)` が発火する。当初案では「親階層（子セット計画を持つ）は Menu、葉階層は直接ボタン」と階層差で UI を分けるアイデアも検討したが、Menu 項目が「削除」1 個だけになると「︙ → 削除」の 2 タップが冗長で、ヘッダーの視覚ノイズも増える。次に階層を超えて「削除は直接 TrashIcon」で統一する案も実装したが、種目計画ヘッダーの TrashIcon と直下のセット計画行の TrashIcon が縦に揃って視認性が落ちる問題が判明した（カード内でゴミ箱2個が並ぶ）。最終的に「親階層（カード単位）の削除は `XMarkIcon` でカードを閉じる」「葉階層（行単位）の削除は `TrashIcon` で行を捨てる」というアイコン語彙の使い分け + 右上角 absolute 配置で SetPlan ゴミ箱と縦並びを物理的に解消。SetPlan / ExercisePlan ともに「直接1タップで削除」という操作モデルの一貫性は維持。種目計画ゼロ件（全削除後のエッジケース）は ExercisePlanSection が `<ol>` を出さず `CreateExercisePlanCard` のみを表示する空状態にする。Cons: 親階層の削除は子セット計画ごと巻き込むため、葉の削除より影響範囲が大きい。誤タップ時のリスクは `design-principles.md` の「安心の設計: undo/redo」を前提に、別途 undo/redo 機能で取り消し可能性を担保する（未実装、別 breakdown `docs/development/2026-05-20-undo-redo/` で追跡）。種目選択 picker（ExerciseSelector）の ExercisePlanRow への統合（旧 2-3-8 で先送りした分）は本判断のスコープ外で別 PR で実装する。`exercise: null` 行は当面「種目を選択」プレースホルダ表示のままになる |
